# Core Django requirements
Django>=3.2.0,<4.3.0
djangorestframework>=3.12.0
djangorestframework-simplejwt>=5.0.0
django-cors-headers>=3.10.0

# Optional dependencies (for full functionality)
# These are in the root directory and may not be available in the Django project
# celery>=5.3.0
# redis>=4.5.0
# reportlab>=3.6.12

# Note: The application will work without the external dependencies
# but some features will return dummy data for testing purposes
