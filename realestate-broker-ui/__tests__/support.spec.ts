import { test } from '@playwright/test'

test.describe('support flows', () => {
  test.skip('settings support contact', async ({ page }) => {
    // placeholder for contact support flow
  })

  test.skip('settings bug report', async ({ page }) => {
    // placeholder for bug report flow
  })

  test.skip('billing consultation request', async ({ page }) => {
    // placeholder for consultation flow
  })
})
